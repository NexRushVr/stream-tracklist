# Local data / per-run output
logs/
output/
*_songs.csv
*_songs.txt
*_matches.jsonl
*.log

# PyInstaller build artifacts (the build is reproducible from build_gui.ps1
# — no need to commit the regenerated .spec or the binaries)
build/
dist/
*.spec
stream-tracklist.exe
gui_debug.log

# Local scheduler artifacts (the *.example.bat versions are committed)
scheduled_daily_scan.bat
daily_streamers.local.txt
stream-tracklist.bat

# Local-only notes (strategy / monetization) — never published
*.local.md

# Local user config + scheduler script (the *.example.* version is committed)
.env
.cache
.spotify_token_cache
scheduled_weekly_scan.bat
scheduled_scan.log

# Stray local media / test clips (the tool processes external files, not in-repo)
test_clip.mp3
*.mp4

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/

# Environments
.venv/
venv/
env/

# IDE / tooling
.vscode/
.idea/
.claude/
.DS_Store
Thumbs.db
